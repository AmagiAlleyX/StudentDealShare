# 数据库配置总结

## ✅ 已完成配置

### 1. SQL 脚本
- **位置**: `scripts/init.sql`
- **内容**: 完整的数据库初始化脚本
- **包含**: 
  - 15 张数据表结构
  - 索引和约束
  - 初始化数据
  - 统计视图

### 2. Maven 依赖配置

#### 父工程 pom.xml
```xml
<!-- MariaDB 驱动 (默认) -->
<dependency>
    <groupId>org.mariadb.jdbc</groupId>
    <artifactId>mariadb-java-client</artifactId>
    <version>3.3.2</version>
</dependency>

<!-- MySQL 驱动 (备用，已注释) -->
<!--
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <version>8.0.33</version>
</dependency>
-->
```

#### sd-mapper 模块 pom.xml
```xml
<!-- MariaDB 数据库驱动 (默认使用) -->
<dependency>
    <groupId>org.mariadb.jdbc</groupId>
    <artifactId>mariadb-java-client</artifactId>
    <scope>provided</scope>
</dependency>

<!-- MySQL 数据库驱动 (备用) -->
<!-- 
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>provided</scope>
</dependency>
-->
```

### 3. 配置文件

#### application-dev.yml (开发环境)
```yaml
spring:
  datasource:
    # MariaDB 驱动 (默认使用)
    url: jdbc:mariadb://localhost:3306/student_deal_share?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    
    # MySQL 配置已注释（备用）
```

#### application-prod.yml (生产环境)
```yaml
spring:
  datasource:
    # MariaDB 驱动 (默认使用)
    url: jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/student_deal_share?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:dealshare}
    password: ${DB_PASSWORD:YourPassword123!}
    driver-class-name: org.mariadb.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    
    # MySQL 配置已注释（备用）
```

### 4. Druid 连接池配置

#### 开发环境
- 初始连接数：5
- 最小空闲：5
- 最大活跃：20
- 监控页面：开启
- SQL 监控：开启

#### 生产环境
- 初始连接数：10
- 最小空闲：10
- 最大活跃：50
- 监控页面：开启
- 性能优化：开启

## 📊 数据库表统计

| 模块 | 表数量 | 表名 |
|------|--------|------|
| 用户模块 | 4 | t_user, t_user_follow, t_user_favorite, t_user_history |
| 优惠模块 | 3 | t_deal, t_category, t_tag |
| 社区模块 | 5 | t_post, t_comment, t_like_record, t_topic, t_post_topic |
| 消息模块 | 2 | t_notification, t_private_message |
| 系统模块 | 4 | t_admin, t_system_config, t_operation_log, t_sensitive_word |
| **总计** | **18** | - |

## 🔧 快速开始

### 1. 创建数据库
```sql
CREATE DATABASE IF NOT EXISTS student_deal_share 
DEFAULT CHARACTER SET utf8mb4 
DEFAULT COLLATE utf8mb4_general_ci;
```

### 2. 执行初始化脚本
```bash
# 使用 MariaDB
mariadb -u root -p student_deal_share < scripts/init.sql

# 或使用 MySQL
mysql -u root -p student_deal_share < scripts/init.sql
```

### 3. 修改数据库配置
编辑 `sd-admin/src/main/resources/application-dev.yml`，配置数据库连接信息：
```yaml
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/student_deal_share
    username: your_username
    password: your_password
```

### 4. 启动项目
```bash
cd student-deal-share/sd-admin
mvn spring-boot:run
```

### 5. 访问 Druid 监控
- 地址：http://localhost:8080/druid/
- 用户名：admin
- 密码：admin

## 🔄 切换数据库驱动

### 从 MariaDB 切换到 MySQL

**步骤 1**: 修改配置文件
```yaml
# 注释掉 MariaDB
# url: jdbc:mariadb://localhost:3306/student_deal_share
# driver-class-name: org.mariadb.jdbc.Driver

# 启用 MySQL
url: jdbc:mysql://localhost:3306/student_deal_share?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
driver-class-name: com.mysql.cj.jdbc.Driver
```

**步骤 2**: 修改 pom.xml
```xml
<!-- 注释掉 MariaDB -->
<!--
<dependency>
    <groupId>org.mariadb.jdbc</groupId>
    <artifactId>mariadb-java-client</artifactId>
</dependency>
-->

<!-- 启用 MySQL -->
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
</dependency>
```

**步骤 3**: 重新编译
```bash
mvn clean install
```

## 📋 配置检查清单

- [x] SQL 脚本创建（18 张表）
- [x] MariaDB 驱动配置（默认）
- [x] MySQL 驱动预留（注释）
- [x] Druid 连接池配置
- [x] 开发环境配置
- [x] 生产环境配置
- [x] 初始化数据准备
- [x] 监控页面配置
- [x] 文档说明完善

## 🎯 配置亮点

1. ✅ **双驱动支持**: MariaDB（默认）+ MySQL（备用）
2. ✅ **强大连接池**: 阿里 Druid，性能卓越
3. ✅ **完整监控**: Druid 监控页面，实时掌握数据库状态
4. ✅ **灵活切换**: 通过注释即可切换数据库类型
5. ✅ **环境分离**: 开发和生产环境分别配置
6. ✅ **性能优化**: PSCache、连接数优化
7. ✅ **文档齐全**: 详细的配置说明和使用指南

## 📖 相关文档

- [数据库配置说明.md](scripts/数据库配置说明.md) - 详细的数据库配置说明
- [init.sql](scripts/init.sql) - 数据库初始化脚本
- [README.md](README.md) - 项目说明文档

## 💡 最佳实践

1. **开发环境**: 使用 MariaDB，开启详细日志和监控
2. **生产环境**: 根据负载选择合适的数据库，优化连接池配置
3. **监控告警**: 配置 Druid 监控，设置告警阈值
4. **定期备份**: 配置定时备份任务
5. **性能调优**: 根据监控结果持续优化

---

配置完成！现在可以开始初始化数据库并启动项目了。
